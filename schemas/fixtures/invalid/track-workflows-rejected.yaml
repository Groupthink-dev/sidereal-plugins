# expected-error: track-workflows-retired
# DD-260 Phase A — pack declares retired top-level track_workflows: field.
# Schema rejects via the top-level not.required:["track_workflows"] constraint.
spec_version: "3.0.0"
pack: "1.13"
slug: track-workflows-fixture
name: track-workflows-fixture
title: "Track Workflows Fixture"
version: "1.0.0"
description: Pack declaring retired track_workflows — must be rejected.

trust_tier: first-party

source:
  type: vault
  vault_path: packs/track-workflows-fixture/

runtime:
  distribution: none
  publish_targets: []

author:
  name: Stallari

data:
  reads: []
  writes: []
  stores: nothing
  phones_home: false

skills:
  - name: noop
    description: noop
    prompt: |
      Respond with 'ok'.

track_workflows:
  - name: legacy-workflow
    states: [open, done]
    initial: open
    terminal: [done]
    verbs:
      open:
        to: open
        permissions: transition
